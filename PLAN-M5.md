# M5 Implementation Plan — Router, logging, edge-cli, CI matrix, docs

**Status: ✅ COMPLETE (2026-08-24)** — `edge-cli` (generate + check) implemented,
the conformance CI matrix added, and the docs written. The full conformance
suite (T1–T8, r1, T11) stays green on all three targets, now running the
Viceroy harness against **edge-cli-generated** `fastly.toml` files (D15
codegen handoff). §14 open question 1 resolved (see §3).

**Goal (spec §12, M5):** Router, logging, `edge-cli`, conformance CI matrix,
docs — full suite green on host + Viceroy + workerd; CI on both wasm targets.

**Definition of done:**
- Router/logging: already delivered in M0/M1 (T3/T9); nothing new needed.
- `edge-cli generate`: deterministic, idempotent `wrangler.toml` + `fastly.toml`
  from `edge.toml` (SPEC §9, D6, D15).
- `edge-cli check`: validates a deployed `fastly.toml` against `edge.toml`
  (origins → backends with target/override_host/use_ssl; stores → `[setup]`).
- CI matrix: host tests/lint/docs + both wasm targets + Viceroy + workerd.
- Docs: root README, edge-cli README, SPEC status + open-question resolution.

---

## 1. What was built

```
edge/
├── edge-cli/                       # NEW workspace member (host-only)
│   ├── Cargo.toml                  # deps: edge-core, toml, serde (+ tempfile dev)
│   ├── README.md
│   └── src/
│       ├── main.rs                 # generate/check subcommands, flag parsing
│       ├── codegen.rs              # deterministic fastly.toml/wrangler.toml
│       └── check.rs                # [setup] vs edge.toml validation
├── .github/workflows/ci.yml        # host / wasm / viceroy / workerd jobs
├── README.md                       # NEW root docs
├── edge-core/src/config.rs         # Stores::binding_count() (check reporting)
├── examples/hello-world/           # fastly.toml regenerated by edge-cli;
│                                   #   wrangler.toml generated
└── tests/conformance/              # fastly.toml regenerated (incl. refused
                                    #   backend added to [setup]); wrangler.toml
```

## 2. Codegen decisions

1. **`[setup.backends]` is keyed by backend name**, not origin alias — the
   resolver looks backends up via `Backend::from_name(origin.backend)`
   (`edge-fastly/src/resolve.rs`). Caught by a unit test, not the spec.
2. **`[local_server]` is preserved verbatim** when the target file already has
   one: it is Viceroy testing config, not derivable from `edge.toml` (D15).
   Regeneration is a fixed point — verified by diffing consecutive runs.
3. **KV namespaces omit `id`** in `wrangler.toml`: wrangler auto-provisions
   the namespace and writes the id back on first deploy (verified against
   the wrangler 4.125.0 config schema).
4. **Determinism**: origins sorted by alias; no map-iteration order leaks.

## 3. §14 open question 1 — resolved

"CF fetch permission syntax": verified 2026-08-24 against wrangler 4.125.0's
`config-schema.json` — **no `permissions`/fetch-allowlist key exists** (only
generic `unsafe.bindings`, which is not a fetch allowlist). Outbound `fetch`
is allow-by-default on Workers, so `edge-cli` emits no permission config and
the §7.5 fail-open-on-CF asymmetry remains documented, not configurable.
SPEC.md §7.2/§14 updated accordingly.

## 4. Verification

| Check | Result |
|---|---|
| `cargo test --workspace` | 22 suites ok, 0 failures (11 new edge-cli tests) |
| `cargo clippy --workspace --all-targets -D warnings` | 0 warnings |
| `cargo fmt --check` | clean |
| `cargo doc --workspace --no-deps` | builds |
| `edge-cli check` (hello-world + conformance) | OK |
| `edge-cli generate` | deterministic, idempotent; fresh output passes `check` |
| `run.sh` (Viceroy) with **generated** fastly.toml | T1–T8, r1, T11 all pass |
| `run-cf.sh` (workerd) | T1–T8, r1, T11 all pass |
| wasm builds (both targets, both services) | clean |

## 5. Risks & notes

- **CI not exercised here** (no GitHub remote push); the workflow mirrors the
  locally-verified commands 1:1. First run may need `viceroy` install tuning
  (cargo install is slow; cargo-binstall is an alternative).
- **Live deploys** remain the only unexercised path (accounts pending): the
  generated `fastly.toml`/`wrangler.toml` are deploy-ready modulo account
  IDs (KV auto-provisioning) and CF vars/secrets (per-environment).
- The hand-maintained `fastly.toml` files are now edge-cli artifacts (regenerate
  in place after any `edge.toml` change; `edge-cli check` guards drift).
