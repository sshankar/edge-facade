[package]
name = "edge-cli"
version = "0.1.0"
description = "Codegen: edge.toml → wrangler.toml + fastly.toml; config validation (SPEC §9)"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

[[bin]]
name = "edge-cli"
path = "src/main.rs"

[dependencies]
edge-core = { path = "../edge-core" }
serde.workspace = true
toml = "0.8"

[dev-dependencies]
tempfile = "3"
