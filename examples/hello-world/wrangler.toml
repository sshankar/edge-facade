# Generated by edge-cli from edge.toml — do not edit by hand.
# Regenerate with: edge-cli generate

name = "hello-world"
main = "build/index.js"
compatibility_date = "2025-08-01"

# KV namespace binding (auto-provisioned: wrangler writes
# the namespace id back here on the first deploy).
[[kv_namespaces]]
binding = "hello_kv"

# Vars/secrets live in platform configs (edge.toml [stores] binds
# them); add values here or via `wrangler secret put`.
[vars]
