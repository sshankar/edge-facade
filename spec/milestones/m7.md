# M7 — wake-capable Fastly executor + deadlines

**Status:** — pending

## Deliverable

Wake-capable Fastly executor + monotonic clock + deadline API (`Context::timeout`/`elapsed`/`remaining`, `TimeoutScope`). Supersedes the D3 poll-loop on Fastly (parking, timer wakeups, handler+timer concurrency, response committed before deferred drain)

## Exit criteria

executor parks rather than busy-spins; timer-driven wakes; P5, P6 on host + Viceroy + workerd

## Related

- no implementation plan yet
- Decisions: [D3](../decisions/d03.md)
- Conformance: [suite](../conformance.md)
- portability pages: [portability/time-deadlines](../portability/time-deadlines.md)
